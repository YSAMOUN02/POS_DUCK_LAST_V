<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <system.webServer>
        <rewrite>
            <rules>
                <rule name="Laravel" stopProcessing="true">
                    <match url="^(.*)$" ignoreCase="false" />
                    <conditions logicalGrouping="MatchAll">
                        <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
                        <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
                    </conditions>
                    <action type="Rewrite" url="index.php/{R:1}" appendQueryString="true" />
                </rule>
                <rule name="Force HTTPS Redirection" enabled="false" stopProcessing="true">
                    <match url="^(.*)$" />
                    <conditions>
                        <add input="{HTTPS}" pattern="^OFF$" />
                    </conditions>
                    <action type="Redirect" url="https://{HTTP_HOST}:{SERVER_PORT}/{R:1}" />
                </rule>
            </rules>
            <rewriteMaps>
                <rewriteMap name="&lt;rule name=&quot;Redirect to HTTPS&quot;>&#xA;    &lt;match url=&quot;(.*)&quot; />&#xA;    &lt;conditions>&#xA;        &lt;add input=&quot;{HTTPS}&quot; pattern=&quot;OFF&quot; />&#xA;    &lt;/conditions>&#xA;    &lt;action type=&quot;Redirect&quot; url=&quot;https://{HTTP_HOST}/{R:1}&quot; redirectType=&quot;Permanent&quot; />&#xA;&lt;/rule>" />
                <rewriteMap name="https://{HTTP_HOST}:{SERVER_PORT}/{R:1}" />
            </rewriteMaps>
        </rewrite>
        <defaultDocument>
            <files>
                <clear />
                <add value="index.php" />
            </files>
        </defaultDocument>
        <handlers>
            <remove name="PHP_via_FastCGI" />
            <add name="PHP_via_FastCGI" path="*.php" verb="*" modules="FastCgiModule" scriptProcessor="C:\PHP\php-cgi.exe" resourceType="Either" />
        </handlers>
        <staticContent>
            <!-- Every static asset URL carries ?v=<filemtime>, so a changed file
                 is a changed URL and the browser refetches it on its own. This
                 was DisableCache, which sent Cache-Control: no-cache on every
                 static file and defeated that: the company logo, the fonts and
                 ~4.7 MB of vendor JS (html2pdf, flowbite, html2canvas, qz-tray)
                 were re-downloaded on every page load and every print window.

                 Only <staticContent> is affected — PHP responses go through
                 FastCGI, so the Blade pages still revalidate every time and no
                 stale page HTML can be served. -->
            <clientCache cacheControlMode="UseMaxAge" cacheControlMaxAge="30.00:00:00" />
        </staticContent>
        <security>
            <requestFiltering>
                <hiddenSegments>
                    <remove segment="bin" />
                </hiddenSegments>
            </requestFiltering>
        </security>
    </system.webServer>
</configuration>
